export const logInfo = (m: string) => console.log(`[INFO] ${m}`);
export const logWarn = (m: string) => console.warn(`[WARN] ${m}`);
export const logError = (m: string) => console.error(`[ERROR] ${m}`);
